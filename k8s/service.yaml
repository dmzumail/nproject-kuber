apiVersion: v1
kind: Service
metadata:
  name: nproject-app-svc
  namespace: default
spec:
  selector:
    app: nproject-app
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8000
  type: ClusterIP